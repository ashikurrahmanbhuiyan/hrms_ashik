spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hrms_ashik_test
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
 
server:
  port: 8080